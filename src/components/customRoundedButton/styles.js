import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: 70,
    height: 70,
    alignItems: 'center',
    borderRadius: 100,
    bottom: 0
  },
});

export default styles;